const BALANCE_OF = [
  "function balanceOf(address account) public view returns (uint256)",
];
const TOTAL_SUPPLY = ["function totalSupply() public view returns (uint256)"];

export default {
  BALANCE_OF,
  TOTAL_SUPPLY,
};
